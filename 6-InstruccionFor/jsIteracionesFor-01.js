//pedir nro hasta q la suma sea igual o mayor a 1000
//informar la cant de numeros pedidos
function mostrar()
{
	for (i = 1 ; i < 11; i++){
		document.write(i);
	}
}
/*let nroIngresado;
	let acumulador = 0;
	let contador = 0;

	while (acumulador < 1000){

		numero = parseInt(prompt("ingrese un nro"));
		while (isNaN(numero)){
		numero = parseInt(prompt("no es un nro valido, ingrese un nro"));
		}
		acumulador += numero;
		contador++;
	}
	alert("se pidieron " + contador + " numeros");*/
	